package com.underdog.aftermath.world.biome;

/*import net.fabricmc.fabric.api.biome.v1.BiomeModifications;
import net.fabricmc.fabric.api.biome.v1.BiomeSelectors;
import net.fabricmc.fabric.api.biome.v1.ModificationPhase;
import net.minecraft.registry.RegistryKey;
import net.minecraft.util.Identifier;
import net.minecraft.world.biome.Biome;
import net.minecraft.world.biome.BiomeKeys;

public class ModBiomesModifier {


    public static void modifyBiomes() {
        RegistryKey<Biome> biomeToRemove1 = BiomeKeys.MANGROVE_SWAMP;

        BiomeModifications.create(new Identifier("aftermath", "remove_biomes"))
                .add(ModificationPhase.REMOVALS, BiomeSelectors.includeByKey(biomeToRemove1)
                        .and(BiomeSelectors.includeByKey(biomeToRemove1)),
                        (ctx) -> {});
    }
}*/